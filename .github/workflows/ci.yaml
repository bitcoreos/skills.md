name: CI
on: [push, pull_request]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Verify Hashes
        run: |
          chmod +x scripts/hash-verifier.sh
          ./scripts/hash-verifier.sh
